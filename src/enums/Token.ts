export enum AdapterToken {
    API = "ApiAdapter",
}

export enum ClientToken {
    DTO = "DTOClient",
    HTTP = "HttpClient",
}

export enum ControllerToken {
    GAME = "GameController",
}

export enum ServiceToken {
    GAME = "GameService",
}
